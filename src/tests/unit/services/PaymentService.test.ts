import PaymentService from '../../../app/services/PaymentService';

describe('Testing PaymentService', () => {
    it('should create new paymentSession', async () => {
        expect(1).toBe(1);
    });
});